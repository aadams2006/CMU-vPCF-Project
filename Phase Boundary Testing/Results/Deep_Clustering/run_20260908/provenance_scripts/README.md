# Historical packaging scripts

These scripts preserve the original workstation packaging process. They expect the original workspace layout and are not the experiment entry point. To reproduce training, use `Phase Boundary Testing/run_deep_benchmark.py`. `initial_test_attempt.log` records an initial Windows temporary-file cleanup failure, fixed before the final 16-test passing run in `tests.log`.
